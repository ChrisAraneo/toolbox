import { createUnitTestData } from '../../utils/create-unit-test-data.function';

export const NO_CLASS = createUnitTestData(
  'export function sum(a, b) { return a+b; }',
  'export function sum(a, b) { return a+b; }',
);
