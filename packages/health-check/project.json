{
  "name": "@chris.araneo/health-check",
  "targets": {
    "build": {},
    "chores": {},
    "format": {
      "options": {
        "commands": [
          "npx sort-package-json package.json",
          "npx prettier --write tsconfig.lib.json tsconfig.json package.json project.json \"*.{ts,js,mjs,cjs}\" \"src/**/*.ts\""
        ],
        "cwd": "{projectRoot}",
        "parallel": false
      }
    },
    "lint": {},
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node --experimental-vm-modules node_modules/jest/bin/jest.js ./packages/health-check --collectCoverage=false"
      }
    },
    "typecheck": {}
  }
}
